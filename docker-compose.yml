services:
  timesand:
    image: ghcr.io/itmwuma/timesand:latest
    user: "0:0"
    ports:
      - "8800:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      - DATA_DIR=/data
