services:
  timesand:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - timesand_data:/data
    restart: unless-stopped
    environment:
      - DATA_DIR=/data

volumes:
  timesand_data:
