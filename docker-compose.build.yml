services:
  timesand:
    build:
      context: .
      dockerfile: Dockerfile
    user: "0:0"
    ports:
      - "8800:8080"
    volumes:
      - ./data:/data
    restart: unless-stopped
    environment:
      - DATA_DIR=/data
