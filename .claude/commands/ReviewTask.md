根据需求文档$ARGUMENTS进行验收工作，步骤如下：
1. 根据当前的实现验收需求（$ARGUMENTS），并总结汇报
    注：可访问127.0.0.1:8080并使用chrome-devtools工具进行测试（如果8080未启动可使用docker compose up --build -d启动服务）
2. 更新docs目录下任务进度，同时提醒用户进行查阅文档
    注：操作obsidian时可使用obsidian cli相关技能

原则：
- 用户未主动声明时不得修改已有代码